import { NoteWithContentFilterPipe } from './note-with-content-filter.pipe';

describe('NoteWithContentFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NoteWithContentFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
